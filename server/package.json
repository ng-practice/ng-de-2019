{
  "name": "todos-server",
  "version": "1.0.0",
  "description": "backend of todos app",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch todos-db.json --routes routes.json"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.15.0"
  }
}
